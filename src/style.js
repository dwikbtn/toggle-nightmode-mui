import { makeStyles } from "@material-ui/core/styles";

const useStyle = makeStyles(() => ({
  container: {
    marginTop: "20px",
  },
  toggleBtn: {
    marginBottom: "20px",
  },
  cardItem: {
    padding: "5px",
  },
  randomBtn: {
    margin: "20px",
    padding: "10px",
    borderRadius: "10px",
  },
}));
export default useStyle;
